default:
  autoload:
    '': '%paths.base%/tests/features'
  suites:
    application:
      paths: 
        - '%paths.base%/tests/features/application'
      contexts:
        - ApplicationContext

    # responses:
    #   paths:
    #     - '%paths.base%/tests/features/responses'
    #   contexts:
    #     - ApplicationContext
